$("#records-partial").html("<%= escape_javascript(render partial: 'records')%>" );
$("#added-record-no").text("<%= escape_javascript(@records.size.to_s) %>");
$("#record-mid").text("<%= escape_javascript(@mid) %>");
$("#record-tid").text("<%= escape_javascript(@tid) %>");
